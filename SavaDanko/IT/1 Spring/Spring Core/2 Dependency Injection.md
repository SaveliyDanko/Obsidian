---
tags:
  - review
sr-due: 2026-03-10
sr-interval: 65
sr-ease: 250
---

---
**Dependency Injection** (внедрение зависимостей) — это паттерн проектирования, при котором объект не создаёт свои зависимости сам, а получает их извне, обычно от фреймворка или контейнера (в Spring — от IoC-контейнера).

```java
public class Car {
    private Engine engine = new Engine(); // 👎 — жёсткая связность
}
```
А с Dependency Injection:
```java
public class Car {
    private final Engine engine;

    public Car(Engine engine) { // 👍 — зависимость приходит извне
        this.engine = engine;
    }
}
```