---
tags:
  - review
sr-due: 2026-04-11
sr-interval: 117
sr-ease: 270
---

---
**Маска подсети** — это параметр, который определяет, какая часть IP-адреса относится к сети, а какая — к устройствам (хостам) внутри этой сети.

**Зачем нужна:**
Она помогает разделять IP-адрес на две части — адрес сети и адрес узла. Благодаря этому устройства и маршрутизаторы понимают, находятся ли два адреса в одной сети или нужно передавать данные через шлюз.

**Пример:**
IP-адрес: `192.168.1.10`  
Маска подсети: `255.255.255.0`  
👉 первые три числа (`192.168.1`) — это сеть,  
последнее (`10`) — это устройство в этой сети.

То есть в этой подсети могут быть адреса от `192.168.1.1` до `192.168.1.254`, а `192.168.1.0` — адрес самой сети, `192.168.1.255` — широковещательный адрес (broadcast).

**Другой формат записи:**  
Маску часто записывают через косую черту — **CIDR**.  
Например: `192.168.1.10/24` (где `/24` означает 24 бита для сети, то есть маска `255.255.255.0`).

**Loopback (обратная связь)**

| Диапазон    | Назначение                                                                          |
| ----------- | ----------------------------------------------------------------------------------- |
| 127.0.0.0/8 | используется для тестирования внутри компьютера (например, `127.0.0.1` — localhost) |

**Broadcast**

| Диапазон        | Назначение                                        |
| --------------- | ------------------------------------------------- |
| 255.255.255.255 | отправка пакета всем устройствам в локальной сети |
