`N` $-$ Transform Object
`Object Mode` $\rightarrow$ `Object` $\rightarrow$ `Apply` $\rightarrow$ `All Transforms` для обнуления `Location` в `Transform`

Приближение к определенному `Frame`:
`View` $\rightarrow$ `Frame Selected`

`X-Ray Mode` $\rightarrow$ `Alt + Z`

`Mirror Modifier` $\rightarrow$ 

`Shift + S` $\rightarrow$ `Cursor to Selected`
`Shift + S` $\rightarrow$ `Selection to Cursor`
