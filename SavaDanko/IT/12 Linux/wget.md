#linux 

---
**`wget`** — это утилита командной строки для неинтерактивной загрузки файлов по протоколам HTTP, HTTPS и FTP. Она позволяет загружать файлы с удалённых серверов.
```zsh
wget [опции] [URL]
```

**Простая загрузка файла:**
```zsh
wget https://example.com/file.txt
```
