#linux 

---
- Это **GUI-программа**, запускаемая в графической оболочке (GNOME, KDE и т.п.)
- Используется для взаимодействия с оболочкой (shell): ты вводишь команды — терминал передаёт их оболочке, оболочка — ядру
- Пример: ты открываешь GNOME Terminal → он запускает `bash` → ты вводишь `ls`