#linux 

[[lsmod]]


---
**Модуль ядра (kernel module)** — это отдельный файл с расширением `.ko`, который содержит код, добавляющий функциональность ядру без необходимости пересборки или перезагрузки системы.

