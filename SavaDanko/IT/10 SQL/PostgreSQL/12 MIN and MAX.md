###### **`MIN()` — минимальное значение**
```sql
SELECT MIN(age) AS youngest FROM users;
```
Вернёт самого молодого пользователя.

###### **`MAX()` — максимальное значение**
```sql
SELECT MAX(salary) AS highest_salary FROM employees;
```
Самая высокая зарплата.
