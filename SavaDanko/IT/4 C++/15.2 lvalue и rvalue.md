#### <mark style="background: #ABF7F7A6;">lvalue (Left Value):</mark>
- Это выражение, у которого есть адрес в памяти.
- То есть что-то, у чего можно взять ссылку (`&` работает).
- Может стоять слева от оператора `=` (но не обязательно).

🔹 Примеры:
```cpp
int x = 10;  // x — это lvalue
x = 20;      // можно присваивать

int* p = &x; // можно взять адрес — &x
```

#### <mark style="background: #ABF7F7A6;">rvalue (Right Value):</mark>
- Это временное значение, которое не имеет имени и не живёт долго.
- Не имеет адреса в памяти (по крайней мере, с точки зрения языка).
- Обычно стоит справа от `=`.

🔹 Примеры:
```cpp
int x = 5 + 3; // 5 и 3 — rvalue, 5 + 3 — тоже rvalue
x = 42;        // 42 — rvalue

int y = x + 2; // x — lvalue, x + 2 — rvalue
```

