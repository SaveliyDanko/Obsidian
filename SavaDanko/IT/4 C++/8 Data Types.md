#### Примитивные типы данных в C++

| Тип данных | Размер  | Описание                                 |
| ---------- | ------- | ---------------------------------------- |
| `bool`     | 1 байт  | Логическое значение (`true` или `false`) |
| `char`     | 1 байт  | Символы ASCII                            |
| `int`      | 4 байта | Целые числа                              |
| `float`    | 4 байта | Вещественные числа одинарной точности    |
| `double`   | 8 байт  | Вещественные числа двойной точности      |
| `void`     | -       | Отсутствие типа (для функций)            |

#### Модификаторы типов
Модификаторы типов уточняют диапазоны и размеры примитивных типов.

| Модификатор | Описание                                                |
| ----------- | ------------------------------------------------------- |
| `signed`    | Знаковое число (положительные и отрицательные значения) |
| `unsigned`  | Беззнаковое число, только положительные значения        |
| `short`     | Уменьшает размер (обычно 2 байта)                       |
| `long`      | Увеличивает размер (обычно 4 или 8 байт)                |
| `long long` | Максимальный размер (обычно 8 байт)                     |

#### Символьный и строковый тип
C++ поддерживает как одиночные символы (`char`), так и строки (`string`).

#### Автоматический тип (`auto`)
C++ позволяет **автоматически** определять тип данных.

**Пример:**
```cpp
auto x = 42;      // int
auto y = 3.14;    // double
auto z = "Hello"; // const char*
```

#### Тип `nullptr_t` (C++11)
Используется для указателей, заменяя `NULL`.

**Пример:**
```cpp
int* p = nullptr;
```

#### ==Массивы (`array`)==
[[10 Массивы]]

#### ==`struct` (структуры)==
Позволяет объединять разные типы данных.

**Пример:**
```cpp
struct Person {
    std::string name;
    int age;
};

Person p = {"Alice", 30};
std::cout << p.name;
```

#### `union` (объединения)
Позволяет хранить **только одно** из нескольких возможных значений.

**Пример:**
```cpp
union Data {
    int i;
    float f;
};

Data d;
d.i = 42;
std::cout << d.i;
```

#### `enum` (перечисления)
Используется для задания набора именованных констант.

**Пример:**
```cpp
enum Color {RED, GREEN, BLUE};
Color c = RED;
```

#### Указатели (`*`)
[[15 Указатели]]

#### Ссылки (`&`)
[[15.1 Ссылки]]

#### decltype
Определяет тип переменной **во время компиляции**.

📌 **Пример:**
```cpp
int a = 5;
decltype(a) b = 10; // b будет типа int
```