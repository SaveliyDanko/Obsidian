В C++ массив (`array`) — это **структура данных**, которая позволяет хранить **несколько элементов одного типа** в **непрерывной области памяти**.

#### <mark style="background: #ABF7F7A6;">1. Объявление и инициализация массива</mark>
В C++ массив можно объявить следующим образом:
```cpp
тип_данных имя[размер];
```

**Пример:**
```cpp
int numbers[5];  // Массив из 5 элементов типа int
```
Этот массив хранит **5 целых чисел** (`int`), но их значения **не определены**.

#### ==Инициализация массива==
Можно **заполнить массив сразу** при объявлении:
```cpp
int numbers[5] = {1, 2, 3, 4, 5};
```

Компилятор **сам определит размер**, если его не указать:
```cpp
int numbers[] = {10, 20, 30};  // Компилятор создаст массив на 3 элемента
```

#### ==Доступ к элементам массива==
Элементы массива индексируются **с 0**.

#### <mark style="background: #ABF7F7A6;">2. Изменение элементов массива</mark>
Значение элементов можно изменять:
```cpp
numbers[1] = 100;  // Заменяем второй элемент (20 → 100)
std::cout << numbers[1];  // Выведет 100
```

#### <mark style="background: #ABF7F7A6;">3. Длина массива (`sizeof`)</mark>
Чтобы узнать размер массива в **байтах**, используем `sizeof`:
```cpp
int arr[5] = {1, 2, 3, 4, 5};

std::cout << "Размер в байтах: " << sizeof(arr) << std::endl;
```

Но нам нужна **количество элементов**, а не байты!

**Формула для длины массива:**
```cpp
int length = sizeof(arr) / sizeof(arr[0]);
```

#### <mark style="background: #ABF7F7A6;">4. Передача массива в функцию</mark>
Массив **не копируется** при передаче в функцию, а передается **указателем**.

📌 **Пример функции для вывода элементов массива:**
```cpp
#include <iostream>

void printArray(int arr[], int size) {
    for (int i = 0; i < size; i++) {
        std::cout << arr[i] << " ";
    }
    std::cout << std::endl;
}

int main() {
    int numbers[] = {10, 20, 30, 40, 50};
    int size = sizeof(numbers) / sizeof(numbers[0]);
    
    printArray(numbers, size);
    
    return 0;
}
```
**Лучше использовать `const int arr[]`** в функции, если массив **не должен изменяться**.

#### <mark style="background: #ABF7F7A6;">5. Двумерные массивы (`matrix`)</mark>
В C++ можно объявлять **массивы внутри массивов**:

**Объявление двумерного массива:**
```cpp
int matrix[2][3] = {
    {1, 2, 3}, 
    {4, 5, 6}
};
```

**Доступ к элементам:**
```cpp
std::cout << matrix[1][2];  // Выведет 6
```
