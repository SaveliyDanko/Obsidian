Разделение на файлы с исходным кодом и заголовочные файлы чисто условное, нет правил, запрещающих использовать .cpp файл как заголовочный.  
  
Не стоит помещать _определения_ в заголовочные файлы без явной необходимости. В C++ есть способы, позволяющие поместить определение в заголовочный файл, не вызвав при этом ошибки компоновщика, но, как правило, это приводит к увеличению объектного файла и программы в целом.

#### <mark style="background: #ABF7F7A6;">Заголовочные файлы в C++</mark>
Заголовочные файлы (`.h`, `.hpp`) в C++ используются для объявления функций, классов и других сущностей, которые затем реализуются в `.cpp` файлах. Они помогают структурировать код, обеспечивать повторное использование и уменьшать зависимости между модулями.

#### <mark style="background: #ABF7F7A6;">Структура заголовочного файла</mark>
Обычно заголовочный файл (`.h`) содержит:
- Объявления функций и классов.
- Константы (`const`, `constexpr`).
- Определения `typedef` или `using`.
- Директивы препроцессора (`#define`, `#pragma once`).

#### Пример заголовочного файла:
📌 **mathutils.h**
```cpp
#ifndef MATHUTILS_H
#define MATHUTILS_H

int add(int a, int b); // Объявление функции
double square(double x);

#endif // MATHUTILS_H
```

#### **Пример файла реализации:**
📌 **mathutils.cpp**
```cpp
int add(int a, int b) {
    return a + b;
}

double square(double x) {
    return x * x;
}
```

#### **Пример использования заголовочного файла в основном файле:**
📌 **main.cpp**
```cpp
#include <iostream>
#include "mathutils.h"

int main() {
    std::cout << "Sum: " << add(3, 5) << "\n";
    std::cout << "Square: " << square(4.2) << "\n";
    return 0;
}
```

#### <mark style="background: #ABF7F7A6;">3. Защита от повторного включения</mark>
При многократном подключении одного заголовочного файла возможны ошибки компиляции из-за дублирования объявлений. Чтобы этого избежать, используют:
##### **1. Include Guards (`#ifndef`, `#define`, `#endif`)**
```cpp
#ifndef FILENAME_H
#define FILENAME_H

// Код заголовочного файла

#endif // FILENAME_H
```
Этот механизм предотвращает повторное включение файла.

##### **2. `#pragma once` (современный вариант)**
```cpp
#pragma once

// Код заголовочного файла
```
Этот способ проще и поддерживается большинством современных компиляторов.

#### <mark style="background: #ABF7F7A6;">4. Разновидности заголовочных файлов</mark>
##### **1. Стандартные заголовочные файлы**
Подключаются через `< >`:
```cpp
#include <iostream>
#include <vector>
#include <cmath>
```
Эти файлы находятся в системных директориях компилятора.

##### **2. Пользовательские заголовочные файлы**
Подключаются через `""`:
```cpp
#include "myheader.h"
```
Компилятор ищет их сначала в текущей папке, затем в стандартных путях.