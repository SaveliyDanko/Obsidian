`services` — ключевая секция в файле `docker-compose.yml`, которая описывает контейнеры, которые будут запущены.

Фактически, каждый `service` — это логическое описание одного контейнера или группы однотипных контейнеров, которые выполняют определённую роль