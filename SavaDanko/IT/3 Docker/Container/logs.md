`docker logs` - позволяет посмотреть вывод (stdout/stderr) контейнера, как если бы ты запускал программу в терминале.
```bash
docker logs my_container
```

**Флаги:**
`-f, --follow` Follow log output
