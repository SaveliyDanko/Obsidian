`docker start` используется для запуска уже созданного контейнера.

**Флаги:**
`--attach (-a)` - Подключает вывод контейнера к терминалу (`stdout`, `stderr`)
`--interactive (-i)` - Оставляет `stdin` открытым — для интерактивного взаимодействия
