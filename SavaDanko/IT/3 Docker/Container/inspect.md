`docker inspect` — это мощный инструмент для получения всей внутренней информации о контейнерe.
```zsh
docker inspect [OPTIONS] NAME|ID [NAME|ID...]
```

---
`-f, --format` string   Format output using a custom template
