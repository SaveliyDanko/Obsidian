`docker kill` - мгновенно завершает работу контейнера, посылая сигнал SIGKILL (жёсткое завершение процесса).

В отличие от `docker stop`, который посылает `SIGTERM` (мягкое завершение) и ждёт завершения, `kill` не даёт приложению шанса "попрощаться" — процесс завершается сразу.