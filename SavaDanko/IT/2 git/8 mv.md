Команда `git mv` используется для переименования или перемещения файлов/папок в репозитории, одновременно обновляя рабочую директорию и индекс.

Это аналог трёх шагов:
```bash
mv old new       # переименование
git rm old       # удаление старого
git add new      # добавление нового
```
