#rust 

---
`rustfmt` is the official code formatter for Rust. It automatically formats your Rust code according to the default style guidelines defined by the Rust community.

```bash
rustfmt main.rs
```

You can also format the entire project:
```bash
cargo fmt
```