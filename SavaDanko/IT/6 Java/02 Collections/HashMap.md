#java 

---
[[Map]]

---
Реализация интерфейса Map: Быстрый доступ по хешу (`O(1)`), нет порядка

###### ==📌 Контракт `equals()` и `hashCode()`==
Для `HashMap`:
- `K` должен корректно реализовывать `equals()` и `hashCode()`, иначе поиск и сравнение будут некорректны.
- Нарушение контракта `equals()/hashCode()` может привести к тому, что ключ будет потерян.

###### ==📌 Создание HashMap с начальной ёмкостью==
```java
Map<String, Integer> map = new HashMap<>(32, 0.75f);
```

| Параметр        | Что означает                                                                       |
| --------------- | ---------------------------------------------------------------------------------- |
| **capacity**    | Начальное количество корзин                                                        |
| **load factor** | Порог заполнения (обычно `0.75`), после которого происходит **расширение таблицы** |
| **threshold**   | `capacity * load factor` — когда надо увеличить размер                             |
