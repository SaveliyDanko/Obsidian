#java 

---
`var` автоматически выводит тип переменной на основе присваиваемого значения.
```java
var имяПеременной = значение;
```
`var` **не** означает, что переменная становится "динамически типизированной" — тип всё равно фиксированный на этапе компиляции.
