Vite — это инструмент сборки, который обеспечивает более быструю и лёгкую разработку современных веб-проектов.

```bash
npm install -D vite
```

```json
"dev": "vite",
"build": "vite build",
"preview": "vite preview"
```
