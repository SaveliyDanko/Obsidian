Тег `<div>` является блочным элементом, то есть он начинается с новой строки и занимает всю доступную ширину.

Тег `<span>` является строчным (inline) элементом, то есть он не переносится на новую строку и занимает только необходимую ширину.